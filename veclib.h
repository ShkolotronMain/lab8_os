// Печатает массив int
void print_vec(int* vec, int size);

// Печатает двумерный массив int
void print_matrix(int** mat, int lines, int cols);

// Освобождает двумерный массив int
void free_matrix(int** mat, int lines);